[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['pip install -r server/requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'cd server && gunicorn app:app --bind 0.0.0.0:$PORT'